name: Deploy Staging

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  staging:
    runs-on: ubuntu-latest
    steps:
      - run: curl -X POST ${{ secrets.STAGING_WEBHOOK }}
