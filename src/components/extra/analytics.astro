---
const { env } = Astro.locals.runtime
---

<Fragment>
  {(import.meta.env.PROD && env.ANALYTICS_ENDPOINT) && (
    <script
      defer
      src={env.ANALYTICS_ENDPOINT}
      data-website-id={env.ANALYTICS_ID}
      is:inline
    />
  )}
</Fragment>