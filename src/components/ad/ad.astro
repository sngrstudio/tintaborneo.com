---
import { Picture } from 'astro:assets'

interface Ad {
  image: any
}

const { image } = Astro.props
---

<Picture
  class='aspect-[4/3] w-full object-contain'
  src={image}
  formats={['avif', 'webp']}
  widths={[360, 1024]}
  sizes='(min-width: 640px) 1024px, 360px'
  alt={''}
/>
