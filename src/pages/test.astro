---
import { fetchDynamicQuery } from '~/data/fetch'

const _test = await fetchDynamicQuery('get-post', {
  id: 'gerak-cepat-selesaikan-tpp-halikinnor-kumpulkan-semua-kepala-opd'
})
---

<pre>{JSON.stringify(_test)}</pre>
